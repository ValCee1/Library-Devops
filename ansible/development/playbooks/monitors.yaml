- name: Install monitoring tools
  hosts: monitors
  remote_user: "{{ new_username }}"
  become: true
  gather_facts: true
  vars_files:
    - "~/private_files/var_files/my_secrets.yml"

  roles:
    - ansible-loki
    - prometheus
    - grafana
