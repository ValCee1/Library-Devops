[Unit]
Description=Prometheus Monitoring System
After=network.target

[Service]
ExecStart=/opt/prometheus/prometheus-{{ prometheus_version }}.linux-amd64/prometheus --config.file /opt/prometheus/prometheus.yml --storage.tsdb.path /opt/prometheus/data
Restart=always
User=root

[Install]
WantedBy=multi-user.target
