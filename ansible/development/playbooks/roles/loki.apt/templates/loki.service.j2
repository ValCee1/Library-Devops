[Unit]
Description=Loki Log Aggregator
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/loki -config.file {{ loki_config_file }}
Restart=on-failure

[Install]
WantedBy=multi-user.target
